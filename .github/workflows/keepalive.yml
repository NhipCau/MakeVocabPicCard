name: Keep Streamlit App Alive

on:
  schedule:
    - cron: "*/10 * * * *"   # 10分ごとに実行
  workflow_dispatch:         # 手動実行もできるように

jobs:
  keepalive:
    runs-on: ubuntu-latest
    steps:
      - name: Curl Streamlit App
        run: |
          curl -A "Mozilla/5.0" -L https://makevocabpiccard.streamlit.app/
